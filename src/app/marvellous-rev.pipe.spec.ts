import { MarvellousRevPipe } from './marvellous-rev.pipe';

describe('MarvellousRevPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvellousRevPipe();
    expect(pipe).toBeTruthy();
  });
});
